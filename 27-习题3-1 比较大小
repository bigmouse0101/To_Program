#include<stdio.h>
int main()
{   
    int a,b,c;
	scanf("%d%d%d",&a,&b,&c);
	if(a<b&&b<c){
		printf("%d->%d->%d",a,b,c);
	}else if(a<c&&c<b){
		printf("%d->%d->%d",a,c,b);
	}else if(b<c&&c<a){
		printf("%d->%d->%d",b,c,a);
	}else if(b<a&&c>a){
		printf("%d->%d->%d",b,a,c);
	}else if(c<a&&b>a){
		printf("%d->%d->%d",c,a,b);
	}else{
		printf("%d->%d->%d",c,b,a);
	}
	return 0;
}
这种题目大可使用三目运算符的:) 只是当时（大一上）的鼠鼠学习C语言尚浅...
